---
phase: 01-foundation-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/v1/webhooks/creem/route.ts, src/lib/services/creem.service.ts]
autonomous: true
requirements: [TECH-06]

must_haves:
  truths:
    - "Creem.io webhooks process successfully without throwing 500s"
    - "Repeated identical webhooks are safely ignored (idempotent)"
  artifacts:
    - path: "src/app/api/v1/webhooks/creem/route.ts"
      provides: "Webhook endpoint for Creem.io"
      exports: ["POST"]
    - path: "src/lib/services/creem.service.ts"
      provides: "Idempotency logic using Redis/Upstash"
  key_links:
    - from: "src/app/api/v1/webhooks/creem/route.ts"
      to: "src/lib/services/creem.service.ts"
      via: "handles business logic"
---

<objective>
Implement idempotent Creem.io webhooks.

Purpose: Prevent infinite billing loops and double-charges if Creem.io retries a webhook delivery.
Output: A secure webhook endpoint and a service layer that checks Upstash Redis for prior processing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-provisioning/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Redis Idempotency Service</name>
  <files>src/lib/services/creem.service.ts</files>
  <action>Create a service that takes a webhook `eventId`, checks Redis (Upstash) using `SETNX` (or equivalent) with an expiration of 7 days. If the key exists, return a status indicating it was already processed.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Idempotency check function exists and uses Redis.</done>
</task>

<task type="auto">
  <name>Task 2: Build Webhook Route</name>
  <files>src/app/api/v1/webhooks/creem/route.ts</files>
  <action>Create the Next.js App Router POST handler. Verify the Creem signature, extract the `eventId`, call the idempotency service. If already processed, return 200 OK immediately. If new, process the subscription update (mock log for now) and return 200 OK.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Webhook route handles signature, idempotency, and returns 200.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] The webhook route correctly imports and uses the Redis service
</verification>

<success_criteria>
- All tasks completed
- Webhook is protected against replay attacks/retries
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-provisioning/01-01-SUMMARY.md`
</output>
