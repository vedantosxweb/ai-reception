---
phase: 01-foundation-provisioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/services/twilio.service.ts]
autonomous: true
requirements: [BIZ-02]

must_haves:
  truths:
    - "System can search Twilio for available local phone numbers"
    - "System can purchase and provision a Twilio number via API"
  artifacts:
    - path: "src/lib/services/twilio.service.ts"
      provides: "Twilio API interactions"
      exports: ["searchAvailableNumbers", "purchaseAndConfigureNumber"]
  key_links: []
---

<objective>
Implement Twilio number provisioning REST API integration.

Purpose: Allow tenants to buy phone numbers directly through our app instead of bringing their own Twilio accounts.
Output: A backend service layer class wrapping the Twilio SDK to search and purchase numbers, auto-configuring our `/api/v1/` webhook URLs.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-provisioning/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Number Search logic</name>
  <files>src/lib/services/twilio.service.ts</files>
  <action>Add a method `searchAvailableNumbers(countryCode, areaCode)` using the `twilio` npm package. Return a formatted list of available numbers and their monthly cost.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Twilio SDK is correctly utilized to search local numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Build Purchase & Webhook Config logic</name>
  <files>src/lib/services/twilio.service.ts</files>
  <action>Add a method `purchaseAndConfigureNumber(phoneNumber)`. It must use the Twilio SDK to buy the number and immediately set the `VoiceUrl`, `SmsUrl`, and `StatusCallback` to our apps absolute production URL (e.g., `https://[DOMAIN]/api/v1/webhooks/twilio/voice`). Use `process.env.NEXT_PUBLIC_APP_URL` as the base.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Number purchase automatically binds the correct webhook URLs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- Twilio service abstracts the complex SDK calls securely
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-provisioning/01-02-SUMMARY.md`
</output>
