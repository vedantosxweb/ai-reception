---
phase: 01-foundation-provisioning
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified: [prisma/schema.prisma, src/app/api/v1/provisioning/route.ts, src/app/(dashboard)/settings/phone/page.tsx]
autonomous: true
requirements: [BIZ-01, BIZ-03, BIZ-04, BIZ-05]

must_haves:
  truths:
    - "Phone numbers are stored in DB attached to specific tenants"
    - "Tenant can search and view available local numbers on the dashboard"
    - "Tenant can trigger a number purchase from the UI"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PhoneNumber model"
      contains: "model PhoneNumber"
    - path: "src/app/(dashboard)/settings/phone/page.tsx"
      provides: "UI for number search and list"
  key_links:
    - from: "src/app/api/v1/provisioning/route.ts"
      to: "src/lib/services/twilio.service.ts"
      via: "calls"
---

<objective>
Tenant Phone Number DB Binding and Provisioning UI.

Purpose: Connect the newly built Twilio service to the database and expose it to the tenant dashboard so they can actually purchase numbers.
Output: Prisma schema updates for PhoneNumbers, a Next.js API route to trigger the service, and a Settings page UI panel.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-provisioning/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema</name>
  <files>prisma/schema.prisma</files>
  <action>Add a `PhoneNumber` model mapping to `tenantId`. Include fields: `number` (String, unique), `status` (String: pending, active, released), `provider` (String: twilio, byon), `createdAt`, `updatedAt`.</action>
  <verify>npx prisma format && npx prisma generate</verify>
  <done>Schema compiles and generates successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Create Provisioning API Route</name>
  <files>src/app/api/v1/provisioning/route.ts</files>
  <action>Create POST and GET handlers. POST accepts an area code, uses `twilio.service.ts` to search, purchase, and configure. Then creates a record in the `PhoneNumber` prisma table linked to the current authed `tenantId`. GET returns the tenant's current numbers.</action>
  <verify>tsc --noEmit passes</verify>
  <done>API Endpoint bridges the Twilio service and Prisma DB.</done>
</task>

<task type="auto">
  <name>Task 3: Build Settings UI</name>
  <files>src/app/(dashboard)/settings/phone/page.tsx</files>
  <action>Create a UI page that fetches and displays the tenant's current purchased numbers (showing status). Add a form to input an area code and click "Search & Buy", which POSTs to the new provisioning API. Also display a small "Bring Your Own Number" section with instructions just linking to their manual config.</action>
  <verify>npm run build succeeds</verify>
  <done>Tenant can visually manage their telephony provisioning.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma generate` succeeds
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- A user can theoretically click a button to provision a Twilio number bound to their account.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-provisioning/01-03-SUMMARY.md`
</output>
