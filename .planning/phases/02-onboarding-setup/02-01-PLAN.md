---
phase: 02-onboarding-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/dashboard/page.tsx, src/app/onboarding/page.tsx]
autonomous: true
requirements: [BIZ-08, INT-04, CXP-04]

must_haves:
  truths:
    - "New tenants are redirected to /onboarding from /dashboard"
    - "The onboarding wizard contains fields for Voice Speed and Welcome SMS"
  artifacts:
    - path: "src/app/onboarding/page.tsx"
      provides: "Updated stepper UI with new fields"
  key_links: []
---

<objective>
Enhance Onboarding Wizard UI and implement Dashboard Redirect logic.

Purpose: Ensure every user completes setup and has control over voice speed and follow-up SMS.
Output: Updated onboarding UI and strictly enforced onboarding routing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-setup/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dashboard Redirect</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>Modify the server component to check `db.aIReceptionist.count({ where: { tenantId } })`. If 0, redirect to `/onboarding` unless the user is already on a subpage that shouldn't be gated (though here /dashboard is the main entry).</action>
  <verify>Check for redirect() calls in the logic.</verify>
  <done>New tenants are forced into onboarding.</done>
</task>

<task type="auto">
  <name>Task 2: Update Onboarding UI Fields</name>
  <files>src/app/onboarding/page.tsx</files>
  <action>Add a 'Voice Speed' input (range 0.5-2) to the Voice step. Add a 'Welcome SMS' switch/toggle to the Review or a new Logic step. Add a System Prompt text area to the AI Personality step.</action>
  <verify>npm run build succeeds</verify>
  <done>UI reflects all Phase 2 configuration requirements.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- UI renders correctly with new fields
- No loops in redirect logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-setup/02-01-SUMMARY.md`
</output>
