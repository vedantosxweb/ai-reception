---
phase: 02-onboarding-setup
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/api/v1/receptionists/wizard/route.ts]
autonomous: true
requirements: [INT-01, INT-04, CXP-04]

must_haves:
  truths:
    - "System prompt and voice speed are saved to the database upon completion"
  artifacts:
    - path: "src/app/api/v1/receptionists/wizard/route.ts"
      provides: "Updated persistence logic"
---

<objective>
Implement AI Receptionist persistence for new fields.

Purpose: Store the user's custom personality and voice settings in the database.
Output: Updated API route capable of handling prompts, speed, and SMS toggles.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-setup/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Wizard POST handler</name>
  <files>src/app/api/v1/receptionists/wizard/route.ts</files>
  <action>Extract `systemPrompt`, `voiceSpeed`, and `enableWelcomeSms` from the request body. Pass them into the `db.aIReceptionist.create` call. Ensure defaults are sensible (e.g., speed=1.0).</action>
  <verify>tsc --noEmit passes</verify>
  <done>Backend persists all new Phase 2 fields.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- Receptionist records contain custom prompts and speed settings
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-setup/02-02-SUMMARY.md`
</output>
