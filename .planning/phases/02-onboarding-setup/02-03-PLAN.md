---
phase: 02-onboarding-setup
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/onboarding/page.tsx, src/app/api/v1/knowledge/upload/route.ts]
autonomous: true
requirements: [RAG-01]

must_haves:
  truths:
    - "Users can upload text snippets as knowledge sources"
    - "Users can upload PDF files which are parsed into text"
  artifacts:
    - path: "src/app/api/v1/knowledge/upload/route.ts"
      provides: "File upload handler"
---

<objective>
Implement Manual Knowledge Base (RAG) upload.

Purpose: Allow tenants without a website (or with internal docs) to train their AI receptionist immediately.
Output: A new file upload API and an enhanced onboarding step for manual data entry.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-setup/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Upload API</name>
  <files>src/app/api/v1/knowledge/upload/route.ts</files>
  <action>Implement a POST route that accepts Multi-part form data. Handle 'text/plain' and 'application/pdf'. For PDF, install `pdf-parse` and extract text. Save to `KnowledgeBaseService.addTextSource`.</action>
  <verify>tsc --noEmit passes</verify>
  <done>API can process manual text and PDF uploads.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Upload in Wizard UI</name>
  <files>src/app/onboarding/page.tsx</files>
  <action>Add a toggle between 'Website Scan' and 'Manual Upload'. If manual, show a Textarea for snippets and a File Input for PDFs. Trigger the new upload API on file selection.</action>
  <verify>npm run build succeeds</verify>
  <done>Wizard UI supports manual RAG data entry.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- Knowledge base is populated with manual data
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-setup/02-03-SUMMARY.md`
</output>
