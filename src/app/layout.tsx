import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Receptionist - Intelligent Voice & Chat Assistant",
  description: "Professional AI Receptionist system with voice integration, CRM, WhatsApp support, and intelligent conversation capabilities.",
  keywords: ["AI", "Receptionist", "Voice", "Chat", "CRM", "WhatsApp", "Automation"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "AI Receptionist",
    description: "Intelligent Voice & Chat Assistant for your business",
    url: "https://chat.z.ai",
    siteName: "AI Receptionist",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "AI Receptionist",
    description: "Intelligent Voice & Chat Assistant for your business",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
